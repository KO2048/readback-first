# v0.4 iteration record

Plan Version: plan-v2. Source: local v0.3 candidate 04bdf53; public v0.1 remains
a separate baseline. Worktree preflight passed for codex/readback-first-v04.

User direction (2026-09-07): recover the current collaboration effect in the
portable Skill, not copy governance formatting. Preserve every-turn application,
semantic coverage, corrections and unresolved items; improve natural formatting,
multiple semantic views, and useful continuation. The supplied Markdown task is
a test case, not an implementation request. No global installation is included.

Owned scope: SKILL.md, PROTOCOL.md, examples/, tests/, docs/, README.md,
README.zh-CN.md, agents/openai.yaml, CHANGELOG.md. Candidate-only branch delivery;
no stable release, merge or live installation without separate evidence/gates.

Static checks verify contract consistency and deterministic examples only.
Runtime and rendering results must be recorded in tests/runtime-matrix.md.
Rollback: retain public main and prior candidate branches; do not replace global
rules during this iteration. Historical v0.2/v0.3 design files remain provenance.

Validation: contract check PASS; 39 deterministic fixtures PASS; diff whitespace
check PASS. Runtime comparison and host rendering remain pending.
Plan Gate plan-v2 condition satisfied by recorded PREFLIGHT PASSED before edits.
